id = "xzedx.easyjump"
name = "EasyJump"
version = "0.4.0"
min_herdr_version = "0.8.2"
description = "Press a key, type a letter, jump to any space, agent, pane, or tab. EasyMotion / Vimium / vim-choosewin style hint labels, drawn right in the Herdr sidebar."
platforms = ["linux", "macos"]

[[build]]
command = ["cargo", "build", "--release"]

# Sidebar mode: labels appear next to every space and agent row in the sidebar,
# a tiny HUD popup captures the key. The action only asks Herdr to open the
# popup, so nothing runs before the popup process itself.
[[actions]]
id = "open"
title = "EasyJump: hint jump (sidebar)"
contexts = ["workspace", "global"]
command = ["sh", "-c", "exec \"${HERDR_BIN_PATH:-herdr}\" plugin pane open --plugin \"${HERDR_PLUGIN_ID:-xzedx.easyjump}\" --entrypoint jump"]

# List mode: one big popup with a pane mini-map and full tab/agent lists.
[[actions]]
id = "open-list"
title = "EasyJump: hint jump (list popup)"
contexts = ["workspace", "global"]
command = ["sh", "-c", "exec \"${HERDR_BIN_PATH:-herdr}\" plugin pane open --plugin \"${HERDR_PLUGIN_ID:-xzedx.easyjump}\" --entrypoint list"]

[[actions]]
id = "back"
title = "EasyJump: jump back to previous pane"
contexts = ["workspace", "global"]
command = ["./target/release/herdr-easyjump", "--back"]

[[panes]]
id = "jump"
title = "EasyJump"
placement = "popup"
width = 64
height = 5
command = ["./target/release/herdr-easyjump"]

[[panes]]
id = "list"
title = "EasyJump list"
placement = "popup"
width = "80%"
height = "70%"
command = ["./target/release/herdr-easyjump", "--list"]
